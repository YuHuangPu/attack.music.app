SELECT f.ID, f.NAME, f.CONTACT, f.ADDRESS, f.MOBILE, f.REMARK
	, f.CREATE_WHO
	, f.UPDATE_WHO
	, DATE_FORMAT(f.UPDATE_DATE,'%Y-%m-%d %T') as "Update_DATE"
FROM FACTORY f
WHERE f.STATUS = 'S' 
ORDER BY f.ID